<UserControl x:Class="XFinder.Views.SidebarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource SidebarBackgroundBrush}">
    <!-- mac 소스 대응: Views/SidebarView.swift (SidebarView — ScrollView > LazyVStack(spacing 1), 패딩 6) -->
    <ScrollViewer x:Name="Scroller" Focusable="False"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <StackPanel x:Name="Host" Margin="6"/>
    </ScrollViewer>
</UserControl>
